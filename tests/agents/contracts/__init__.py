"""Pydantic v2 contracts shared across the agent functional tests.

Each module here defines the minimal data shapes one agent's contract test
needs — keeping them outside the test files makes the contracts auditable
without scrolling through assertion code.
"""
